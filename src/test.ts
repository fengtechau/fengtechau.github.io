/**
 * Test bootstrap for the Vitest-based Angular unit-test builder.
 *
 * The `@angular/build:unit-test` builder initializes the Angular testing
 * environment itself, so this file intentionally contains no
 * `TestBed.initTestEnvironment` call.
 */
